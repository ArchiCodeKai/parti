# PARTI · 開發路線圖（任務包）

> 版本：2026-07-03 · 每個任務包設計為「一個普通模型可在單一 session 獨立完成」。
> 執行任何任務包前必讀：根目錄 `CLAUDE.md` → 本資料夾 02/03 兩份規格書。
> 完成標準一律含通用門檻：`npm run type-check`、`npm run lint`、`npm run build` 全過。
> 任務包之間的依賴已標明；同 Phase 內無依賴者可平行。

---

> 進度標記：✓ = 已完成（含日期）。執行者完成任務包後在此處補標記。

## Phase 0 · 誠實校正（半天，全部無依賴）— ✓ 全部完成 2026-07-03

### T0.1 對外數字改由資料導出 ✓ 2026-07-03

- **範圍**：`src/components/landing/LandingSections.tsx`（entries 的 count、randomPicks）、
  `src/app/layout.tsx`（metadata description）、`src/app/architects/page.tsx`（「30 位」文案）。
- **做法**：count 改為 `ARCHITECTS.length`、`BUILDINGS.length`、`MOVEMENTS.length` 導出
  （landing 是 client 元件可直接 import 資料）；randomPicks 改為從 ARCHITECTS 取
  importance ≥ 4 者的確定性子集（不用 Math.random，可用日期無關的固定切片）。
- **驗收**：全站 grep 不到 `100+`、`600+`、`200+`、`30 位` 等寫死數字；
  randomPicks 每個名字都能在 `ARCHITECTS` 中找到對應 id。

### T0.2 npm scripts：test + validate ✓ 2026-07-03
（附註：validate 首跑即抓到 pompidou → richard-rogers 斷鏈、已依規格書 §3.3
把 coArchitects 定為前向引用不檢查）

- **範圍**：`package.json`、新建 `scripts/validate-data.ts`。
- **做法**：`"test": "node --test src/**/*.test.mts"`（或等效 glob）；
  `"validate": "npx tsx scripts/validate-data.ts"`。validate 內容照
  技術規格書 §3.3 的資料不變量（字數檢查先留 TODO 註解，Phase 3 啟用）。
- **驗收**：`npm test` 綠；`npm run validate` 過；手動把某個 building id 改錯再跑
  validate 會非零退出並指出斷鏈位置（驗完改回來）。

### T0.3 移除未使用依賴 ✓ 2026-07-03（使用者已授權）

- **範圍**：`package.json` 移除 `mapbox-gl`、`@types/mapbox-gl`、`react-force-graph-2d`。
- **前置**：向使用者列出清單並取得同意（restrictions 規定套件變動需確認）。
- **驗收**：grep 全 src 無引用；`npm run build` 過。**保留 d3-delaunay**（T2.2 要用）。

### T0.4 restrictions.md 對齊現況 ✓ 2026-07-03（使用者已授權）

- **範圍**：`.ai-context/global/restrictions.md`（規範禁止 AI 擅改此目錄，須先取得同意）。
- **做法**：GSAP/Three.js 禁令改為「允許，條件見 docs/00-總規格/03-技術規格書.md AD-3/AD-4」；
  加註「與 docs/00-總規格/ 衝突時以後者為準」。
- **驗收**：檔案內不再出現與現實矛盾的禁令。

## Phase 1 · SEO 架構矯正（v1 最重要工程，先於一切內容工作）— ✓ 全部完成 2026-07-03

### T1.1 architects/[slug] 改 SSG ✓ 2026-07-03（樣板已建立、T1.2 照抄此模式）

> 實作備註：islands 放在 `src/app/architects/[slug]/`（TagBar / BodyGrid /
> CompareButton / Pullquote / movementColors.ts）。CompareButton 用 mounted guard
> 處理 zustand persist 的 hydration；BodyGrid 收 server 渲染的 children。
> 教訓：client 元件不可 import `@/lib/data/*`（會把整包資料拉進 bundle、
> 首頁曾因此 +36kB）— 一律 server 算好傳 props。

- **範圍**：`src/app/architects/[slug]/page.tsx` 拆分。
- **做法**：
  1. `page.tsx` 改 Server Component：移除 `"use client"`、`useParams` 改收 `params` prop、
     加 `generateStaticParams()`（回傳全部 architect id）與 `generateMetadata()`
     （title：`{name.en} {name.zh} · PARTI`、description 取 bodyText 前 80 字）。
  2. 互動塊抽成同目錄 client 元件：`TagBar.tsx`（Hidden-until-Touched peek）、
     `RelatedSidebar.tsx`（scroll 觸發展開）、`CompareButton.tsx`（zustand）、
     `Pullquote.tsx`（IntersectionObserver）。資料以 props 傳入，client 元件不 import data。
  3. slug 不存在改用 `notFound()`。
- **驗收**：build 路由表該路由為 `●`（SSG）；`view-source` 能看到 bodyText 全文；
  瀏覽器分頁 title 為人名；tag bar peek、sidebar 展開、加入比較行為與改動前一致。

### T1.2 buildings/[slug]、movements/[slug] 改 SSG（依賴 T1.1 的模式）✓ 2026-07-03

> 實作備註：buildings 頁除 `Link` 外無任何互動，改 Server Component 後不需 island
> （page JS 降為 162 B）；movements 頁 RelationGeometry 維持 client、architects 由 server
> 端算好傳 props。兩頁 not-found 改用 `notFound()`、`params` 改 `Promise` 型別 await。

- **做法**：照 T1.1 的拆法複製到兩個路由；movements 的 RelationGeometry 維持 client 元件、
  架構師陣列由 server 端算好傳入。
- **驗收**：同 T1.1，三個詳情路由全部 `●`。預渲染 HTML 檔數 = 資料量（29/68/27），
  每頁 title 為條目名、bodyText 進靜態 HTML。

### T1.3 列表頁 metadata + sitemap ✓ 2026-07-03

> 實作備註：四個列表頁（architects/buildings/movements/map）仍是 client 元件、
> 無法直接 `export const metadata`，改由各段落新增 server 端 `layout.tsx`（只回傳
> children）提供 metadata，page.tsx 不動。新增 `src/lib/site.ts` 集中 `SITE_URL`
> （env `NEXT_PUBLIC_SITE_URL`，預設 parti.studio），root layout 補 `metadataBase`。
> sitemap URL 由資料長度導出（禁寫死）。

- **範圍**：四個列表頁加 metadata（透過 layout.tsx）；新建 `src/app/sitemap.ts`、
  `src/app/robots.ts`、`src/lib/site.ts`；root layout 補 `metadataBase`。
- **驗收**：build 後 `/sitemap.xml` 共 130 條 = 6 靜態路由 + 29/68/27 詳情頁；
  列表頁 title 為「建築師/建築/流派/建築地圖 · PARTI」；`/robots.txt` 指向 sitemap。

## Phase 2 · 功能補完（依賴 Phase 1 完成，內部可平行）— ✓ 全部完成 2026-07-08

### T2.1 Cmd+K 統一索引 ✓ 2026-07-07

> 實作備註：`SearchDoc` 維持規格精確 shape；空 query 的 importance 排序清單改由
> search 模組匯出 `DEFAULT_DOCS`（避免把 importance 塞進 SearchDoc）。分組用固定
> 順序（人物→建築→流派）攤平成 rows，鍵盤導航走攤平後的 flatDocs。bundle 零增長
> （三類資料早被 landing client-bundle，webpack 去重）。

- **範圍**：新建 `src/lib/search/index.ts`；改 `src/components/layout/CmdKDialog.tsx`。
- **做法**：統一 `SearchDoc { id, type, route, nameZh, nameEn, tags, body }`；
  結果依 type 分組顯示（人物/建築/流派 + badge）；Enter 跳 `doc.route`。
- **驗收**：搜「落水」→ 出現落水山莊、Enter 跳 `/buildings/fallingwater`；
  搜「brutal」→ 出現粗獷主義流派；空 query 仍顯示 importance 排序的建築師。

### T2.2 RelationGeometry 真幾何 ✓ 2026-07-07

> 實作備註：幾何數學抽成純模組 `src/lib/geometry/relation.ts`（circumcircle 外心公式、
> d3-delaunay 的 delaunayEdges/voronoiCells、layoutPoints 出生年排序決定角度）。
> 關鍵：node --test 無法解析 `@/` 值匯入，故純模組只依賴 d3-delaunay + `import type`
> （會被剝除），離散度由 RelationGeometry 算好傳入 layoutPoints（符合 §4 職責）。
> `hsla(5,…)` 改 `var(--accent-red)` + fillOpacity；force-graph 分支移除、N≥7 一律 Voronoi。
> 測試檔 `relation.test.mts` 5 案全綠。

- **範圍**：`src/components/geometry/RelationGeometry.tsx` + `src/lib/geometry/relation.ts` + 新測試檔。
- **做法**：照技術規格書 AD-6 —— 三點外心公式、d3-delaunay 的 Delaunay/Voronoi、
  點位由出生年排序決定角度；刪除 `hsla(5,…)` 硬編（改 token + opacity）；
  刪除 force-graph 分支（v1 不會出現 N≥13）。
- **驗收**：新增 node:test — 外接圓圓心到三點距離相等（誤差 < 1e-6）、
  Voronoi cell 數 = N；`/compare` 選 3 人時圓確實通過三個點。

### T2.3 樣式收斂 ✓ 2026-07-07

> 實作備註：實際 .tsx 硬編 hex 只剩 2 處（非 18，先前已清）——`#FCFBF8`（theme-color
> meta 需字面值）移到 `src/lib/site.ts` 的 `THEME_COLOR`；`#fff` 改 `var(--bg-page)`
> （專案白字慣例，見 compare-bar__go）。新增 `.page-shell`/`.meta-label` 至 themes.css、
> 套進三詳情頁（buildings 保留 maxWidth 1100）。刪 `movementColors.ts`，TagBar/TimelineBar
> 改讀 `getMovementColor()`（movements.ts 既有）——關鍵：色值由 **server 端 page.tsx 解析後
> 當 prop 傳給 client TagBar**，避免資料進 client bundle（architects/[slug] First Load 維持
> 109kB）。`getMovementColor` 依此改為單一來源 colorTheme，流派色若與舊 map 不同屬刻意矯正。
> cursor:none 全域規則 globals.css 早已存在，本包移除 11 處冗餘 inline。

- **範圍**：`src/app/themes.css`、`src/lib/site.ts` + 各頁面 / islands。
- **做法**：抽 `.page-shell`、`.meta-label`；硬編 hex 改 token / 常數；
  刪除頁內 `MOVEMENT_COLORS`，改讀 `movements.ts` 的 `colorTheme`（server 解析傳 prop）；
  移除冗餘 inline `cursor: none`（全域規則已存在）。
- **驗收**：`grep -rE "#[0-9A-Fa-f]{3,6}" src/app src/components --include="*.tsx"` 為 0 ✓；
  詳情路由全 `●`、First Load 無退步。

### T2.4 RWD pass ✓ 2026-07-08（結構完成，像素目視待使用者 dev server 確認）

> 實作備註：新增響應式格線 class 至 themes.css：`.mosaic-grid`（4→3@900→2@600 欄）、
> `.detail-body-grid`（sidebar 寬走 `--sidebar-col` CSS 變數，≤768 單欄堆疊、aside 改 static）、
> `.decade-row`+`.decade-label`（≤768 堆疊、label 去 sticky）、`.compare-grid`（≤768 單欄）。
> 所有 auto-fill grid 改 `minmax(min(Xpx,100%),1fr)`——桌機零變化、窄寬不溢出、免 media query。
> TagBar 加 `touchstart` 觸發路徑。map（svg width:100% + 860 斷點）、header/compare-bar
> （640 斷點）本已響應式。**依 CLAUDE.md 未啟動 dev server，像素級三檔目視留給使用者。**

- **範圍**：`themes.css` + architects/buildings/movements/compare 各頁 + BodyGrid/TagBar。
- **做法**：馬賽克斷點制；詳情頁 sidebar 手機堆疊；auto-fill grid 防溢出；touch 觸發路徑。
- **驗收**：type-check/lint/build/test 全過 ✓；結構上 375/768/1280 無溢出來源（min() + 斷點）。

### T2.5 資料與型別體質（小包，可穿插）✓ 2026-07-08

> 實作備註：Entity union 移除 BaseEntity；`formatLifespan()` 收斂 3 處顯示用 `>= 2099`
>（[slug] TimelineBar 的 lifeRange 數值計算保留 `>= 2099`，非顯示）；importance 重評
> 保留 6 席 5：柯比意/密斯/萊特/康 + 蓋瑞(解構)/文丘里(後現代奠基)，其餘 11 降 4
>（**主觀策展、可調整**）；分布 5:6 / 4:21 / 3:2。compare 頁 zustand 改 4 個 selector。
> TagBar peek timer useEffect（空依賴、stable refs）本就正確，T1.1 已處理。

- **範圍**：`src/types/entity.ts`、`src/lib/utils.ts`、`src/lib/data/architects.ts`、
  `src/app/page.tsx`、`architects/page.tsx`、`architects/[slug]/page.tsx`、`compare/page.tsx`。
- **做法**：Entity union 去掉 BaseEntity；新增 `formatLifespan()` 取代顯示用 `>= 2099`；
  importance 依配額重評（5 至多 6 席）；zustand 訂閱改 selector。
- **驗收**：type-check 過 ✓；`/architects` 馬賽克 L/M/S 層次（6/21/2）✓；importance=5 = 6 ✓。

### T2.6 a11y pass ✓ 2026-07-08

> 實作備註：map 圖釘 `<g>` 加 `role="link"`/`tabIndex=0`/`aria-label`/`onKeyDown`(Enter+Space)
> /`onFocus/onBlur`(同步 hover 標籤)——68 個圖釘全部可聚焦（驗證進靜態 HTML）。CmdK input 加
> `role="combobox"`/`aria-controls`/`aria-activedescendant`/`aria-autocomplete`，結果容器
> `role="listbox"`+id，每列 `role="option"`/`aria-selected`。themes.css 加全站 `:focus-visible`
> 朱紅 outline（landing 自有 focus 樣式因較高 specificity 保留）。

- **範圍**：`src/app/map/page.tsx`、`CmdKDialog.tsx`、`themes.css`。
- **做法**：map 圖釘可聚焦 + Enter；CmdK 加 combobox/listbox/aria-activedescendant；
  全站 `:focus-visible` 樣式。
- **驗收**：純鍵盤可完成「開 CmdK → 搜尋 → 跳轉」與「map 選一棟建築進入詳情」✓
  （map 圖釘 role=link/tabindex=0 進靜態 HTML；CmdK ↑↓+Enter 導航 + listbox 語意）。

## Phase 3 · 內容深化（依賴 T1.x 完成後開始，可長期平行）

> 內容任務需要使用者參與定稿 —— 模型產草稿、人審。寫作規範：
> `.ai-context/global/content-writing.md` + `docs/05-內容架構/word-count-rules.md`。

### T3.1 內容管線 ✓ 2026-07-08

> 實作備註：啟用前量測全站基線 **0/124 條目在 ±15% 內**（12–46%），故字數檢查採
> **WORD_COUNT_ENFORCED 名單制**（名單內 = error、名單外彙總資訊行不擋驗證），
> 每批擴寫完成把 id 加入名單、棘輪只加不減。字數 = 去空白字元數（中英數標點均計 1）。
> 試點：le-corbusier 照 800 字模板跑完全流程（草稿 785 字、禁用詞 0 命中、年份對齊
> 資料檔——順帶修正舊文「薩伏伊別墅 (1929)」與資料 completed 1931 的不一致）、
> 已入 enforced 名單、validate 綠。**草稿待人審**（checklist 流程第 3 步）。

- **做法**：建 `docs/00-總規格/content-checklist.md`：五步流程（草稿→自查→人審→
  入資料→收緊驗證）、字數計算規則、事實查核清單、可 grep 禁用詞、譯名高風險對照
  （唯一權威 = 資料檔 name.zh）、四型 prompt 模板、現況基線與批次順序；
  `validate-data` 啟用字數 ±15% 檢查（名單制分批啟用）。
- **驗收**：任一位建築師跑完模板流程後，validate 字數檢查通過 ✓（le-corbusier）。

### T3.2–T3.4 條目擴寫（分批任務包，每包 5–10 條）

> T3.2 進度：四大師草稿全部完成（2026-07-08）——柯比意 785 / 密斯 718 / 萊特 708 /
> 康 705 字，禁用詞 0 命中，全部入 WORD_COUNT_ENFORCED、validate 綠。
> **狀態：待人審定稿**（checklist 第 3 步）。順帶修正三處舊文與資料檔的年份不一致：
> 薩伏伊 1929→1931 落成、落水山莊 1935→1937 落成、達卡國會 1983→1982。
>
> T3.3 進度（2026-07-08）：使用者裁示——原 11 位 wordCount=800（丹下/蓋瑞/文丘里/安藤/
> 庫哈斯/赫爾佐格/札哈/卒姆托/SANAA/王澍/伊東）**統一降為 500**（符合寫作規範 Pritzker=500）；
> wordCount 現為 4 大師×800 + 25×500。使用者已確認：邁爾卒年 2025 屬實、資料檔外生平事實 OK。
> **Batch 1 + 2 完成**：14 位原生 500 字組全部草稿完成（強生/巴拉岡/貝聿銘/邁爾/尼邁耶/羅西/
> 西薩 + 槙文彥/莫內奧/皮亞諾/福斯特/穆卡特/坂茂/凱雷）。
> **Batch 3 完成**：11 位（丹下/蓋瑞/文丘里/安藤/庫哈斯/赫爾佐格/札哈/卒姆托/SANAA/王澍/伊東）。
> **T3.3 全部完成（2026-07-11）——29 位建築師內容全數達標**（500 字 ±15%、禁用詞 0），
> 加 T3.2 四大師，architects 全清、enforced **29/124**、validate 綠。**全數待人審**。
> 教訓（給後續批次）：中文去空白字數比直覺短 15–20%，草稿要刻意寫到目標的 1.1–1.15 倍。
> 剩 **T3.4：68 buildings（200–300 字）+ 27 movements（200–1000 字）= 95 條**。

- T3.2 四大師 800 字（柯比意/密斯/萊特/康）→ T3.3 Pritzker 500 字 × 25
  → T3.4 buildings / movements 按字數規格。
- **每包驗收**：validate 過；人工抽查一條的事實正確性；無禁用詞。

## Phase 4 · 出貨（依賴 Phase 1–3）

### T4.1 分享與識別

全站預設 OG image（靜態一張，幾何語言構圖）、favicon、`manifest.ts`。
**驗收**：社群分享偵錯工具顯示正確卡片。

### T4.2 效能驗證

**驗收**：桌機 Lighthouse Performance / SEO / a11y ≥ 90 / 95 / 95；
First Load JS 首頁 ≤ 180kB。

### T4.3 部署 + 投稿

Vercel 上線、自訂網域、Search Console 提交 sitemap、Awwwards 投稿素材。

---

## 執行順序總覽

```
Phase 0（半天）──┬─ T0.1 T0.2 平行；T0.3 T0.4 待使用者同意
                 ▼
Phase 1（2–3 天）── T1.1 → T1.2 → T1.3
                 ▼
Phase 2（3–5 天）── T2.1–T2.6 平行
Phase 3（長期）  ── T3.1 → T3.2 → T3.3 → T3.4（與 Phase 2 平行）
                 ▼
Phase 4（1–2 天）── T4.1 → T4.2 → T4.3
```

**時程哲學**：Phase 1 在任何內容工作之前（SEO 不修好，內容白寫）；
Phase 3 是唯一無法壓縮的長尾，越早開始滾動越好。
